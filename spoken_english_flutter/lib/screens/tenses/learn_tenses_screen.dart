import 'package:flutter/material.dart';

class LearnTensesScreen extends StatelessWidget {
  const LearnTensesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Learn Tenses")),
      body: const Center(
        child: Text("Tense theory content goes here..."),
      ),
    );
  }
}
